# Outputs

Generated outputs are intentionally not committed by default. Run:

```bash
hytes-calval-demo --out outputs --n 2500 --seed 42
```

The current demo uses synthetic HyTES/ECOSTRESS-like matchups, not real NASA or buoy data.
